"""
URL Configuration for Video Matching app
"""
from django.urls import path

app_name = "video_matching"

urlpatterns = [
    # TODO: Add video matching URLs
    # path('upload/', views.VideoUploadView.as_view(), name='upload'),
    # path('matches/<int:content_id>/', views.VideoMatchesView.as_view(), name='matches'),
]
